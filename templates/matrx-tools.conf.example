# .matrx-tools.conf — Project configuration for matrx-dev-tools
# Docs: https://github.com/armanisadeghi/matrx-dev-tools
#
# Place this file in your project root. The installer creates it automatically
# if you run `install.sh` without one.

# ─── Project Type ────────────────────────────────────
# "node" = registers commands in package.json
# "python" = registers commands in Makefile
PROJECT_TYPE="node"

# ─── Tools to Install ───────────────────────────────
# Comma-separated list of tools from the tools/ directory
TOOLS_ENABLED="env-sync"

# ─── Env Sync — Single Config ───────────────────────
# Most projects: one Doppler project, one config, one env file
DOPPLER_PROJECT="my-project"
DOPPLER_CONFIG="dev"
ENV_FILE=".env.local"

# ─── Env Sync — Multi Config (monorepos) ────────────
# For monorepos with multiple Doppler configs (e.g., web + mobile)
# Uncomment and configure:
#
# DOPPLER_MULTI="true"
# DOPPLER_CONFIGS="web,mobile"
#
# DOPPLER_PROJECT_web="my-project"
# DOPPLER_CONFIG_web="web"
# ENV_FILE_web="web/.env.local"
#
# DOPPLER_PROJECT_mobile="my-project"
# DOPPLER_CONFIG_mobile="mobile"
# ENV_FILE_mobile="mobile/.env"
